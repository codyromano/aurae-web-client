import React from 'react';
import './SlideOutContent.css';

const SlideOutContent = ({ children }) => (
  <div className="slide-out-content">
    {children}
  </div>
);

export default SlideOutContent;
