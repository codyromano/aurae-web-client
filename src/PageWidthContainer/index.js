import React from 'react';
import './PageWidthContainer.css';

const PageWidthContainer = ({ children }) => (
  <div className="page-width-container">
    {children}
  </div>
);

export default PageWidthContainer;
